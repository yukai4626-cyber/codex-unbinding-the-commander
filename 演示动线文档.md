# 约 2 分 55 秒评审演示动线

> 项目：STEM教师教育“教-学-研”一体化智能体 · 前端演示系统  
> 用途：不超过 3 分钟的录屏、评审现场演示与答辩讲解  
> 版本：V2.0 ｜ 2026-08-27

这条路线重点证明三件事：教案可以真实编辑，结果可以跨页面流转，后端不可用时仍能依靠 Mock 稳定完成演示。

---

## 一、演示前准备

1. 使用 1920×1080 或接近比例的窗口，浏览器缩放保持 100%。
2. 统一使用 8502，确认 `/_stcore/health` 返回 `ok`，日志没有 traceback。
3. 录屏前完整预走一次并重置工作台流程。
4. 后端尚未完成或现场网络不稳定时，使用 Mock 模式。页面和成果数据必须明确显示“Mock 示例数据”，不要称为真实实证结果。
5. 如演示真实后端，提前设置 `STEM_MOCK_MODE=false`、`STEM_API_BASE` 和可选的 `STEM_API_TOKEN`，并逐接口预热。
6. 演示中只使用内置样例或脱敏文本，不展示测试 Token、真实师生个人信息或后台日志。

---

## 二、时间总表

| 顺序 | 页面 | 时长 | 演示目标 |
|---|---|---:|---|
| 1 | 首页·项目总览 | 20 秒 | 说明项目定位、四大智能体和整体架构 |
| 2 | 跨学科课程设计工作台 | 50 秒 | AI 初生成、真实编辑、提交人工迭代、素养校验 |
| 3 | 跨学科教学模拟实训 | 25 秒 | 接收工作台教案片段，触发一轮课堂反馈 |
| 4 | 智能教学诊断与反思 | 30 秒 | 接收完整教案，生成循证报告和溯源子图 |
| 5 | 教育研究孵化助手 | 20 秒 | 带入诊断结论，生成研究方案 |
| 6 | 技术底座·知识图谱引擎 | 20 秒 | 展示 6 子域、跨域关系和节点详情 |
| 7 | 成果与价值 | 10 秒 | 总结闭环价值，说明数据为 Mock 示例 |

**合计约 175 秒，即 2 分 55 秒。** 讲解时不要逐字念页面，优先保证操作完整。

---

## 三、逐页操作与讲解词

### 1. 首页·项目总览（20 秒）

操作：

- 展示项目标题、整体架构和四大智能体。
- 点击主线入口进入“04 跨学科课程设计工作台”。

参考讲解：

> 这是面向 STEM 师范生培养的“教—学—研”一体化智能体。四个业务智能体共享知识图谱与课堂数据，不只是生成一次内容，而是让课程设计、教学模拟、循证诊断和教育研究连续流转。

### 2. 跨学科课程设计工作台（50 秒）

操作：

1. 使用学段“初中”、主题“碳中和·跨学科项目式学习”、4 课时。
2. 点击“AI 初生成”，展示完整 Markdown 教案。
3. 在可编辑教案中补入一句具体修改，例如：“各小组需比较两种减排方案的成本、效果与公平性。”
4. 点击“提交人工迭代”，强调接口收到的是用户编辑后的完整教案，而不是固定追加动画。
5. 点击“素养校验”，展示 ITRS 四维、STEM 五维和校验结论。
6. 点击“送入教学模拟”。系统把教案前 300 字写入模拟页、重置模拟状态并切换到页面 02。

参考讲解：

> 工作台采用严格三步的人机协同机制。AI 先提供起点，师范生直接编辑内容，再把当前完整版本提交给智能体迭代，最后进行素养对齐校验。人的判断保留在流程中心，而不是由系统自动替代。

### 3. 跨学科教学模拟实训（25 秒）

操作：

1. 指出授课文本已经由工作台教案自动预填，并且仍可编辑。
2. 点击“开始模拟授课”。
3. 点击“触发一轮学生互动”。
4. 展示虚拟学生反馈、四项心流指标和底部多模态信号。
5. 通过侧边栏返回页面 04，点击“送入智能诊断”。系统把完整教案（最多 20,000 字）写入诊断页并切换到页面 03。

参考讲解：

> 同一份教案进入虚拟课堂后，系统按轮次返回不同学生画像、认知负荷、参与度和困惑度。开始授课只初始化课堂，真正点击互动时才请求一次接口，因此页面重跑不会重复调用后端。

### 4. 智能教学诊断与反思（30 秒）

操作：

1. 指出输入区显示“工作台教案”来源，内容是刚才的完整教案。
2. 点击“生成诊断报告”。
3. 快速展示问题、课堂证据、反向归因、改进建议与理论锚点。
4. 点击“查看问题、理论与案例溯源”，展示本次报告对应的局部子图后关闭弹窗。
5. 切换到页面 05“教育研究孵化助手”。

参考讲解：

> 诊断不是只给一个分数，而是把问题连接到课堂证据、教育理论和支撑案例。报告与溯源子图在同一次生成中缓存，因此切换页面或打开弹窗不会重复请求。

补充说明：诊断页本轮支持粘贴纯文本、TXT 上传和内置样例。TXT 由前端读取后只发送纯文本；不演示或承诺 DOCX/PDF 解析。

### 5. 教育研究孵化助手（20 秒）

操作：

1. 点击“带入最近诊断结果”。系统汇总诊断问题、归因结论和建议，最多带入 2,000 字，但不会自动生成。
2. 指出来源标签为最近诊断结果，文本仍可人工修改。
3. 点击“生成研究方案”。
4. 快速切换“研究选题”“文献综述提纲”“问卷与实验设计初稿”三个标签。

参考讲解：

> 诊断中发现的真实教学痛点不会停留在报告里，而是继续转化为研究选题、文献框架、问卷和实验方案，形成从教学问题到教育研究的闭环。

### 6. 技术底座·知识图谱引擎（20 秒）

操作：

1. 通过侧边栏进入页面 06。
2. 展示 61 实体、61 关系和 6 个知识子域。
3. 切换“跨学科概念与能力映射”子域，选择“碳中和”或一个理论节点查看详情。

参考讲解：

> 图谱把学科概念、教育理论、课程标准、教学案例、能力指标和成长证据连接起来，为前面四个智能体提供统一的语义底座。本轮图谱页使用内置数据，不宣称已经连接后端图谱接口。

### 7. 成果与价值（10 秒）

操作：

- 进入页面 07，停留在四大智能体价值、ITRS 前后测图和成长轨迹。
- 确保画面中的“Mock 示例数据”标识可见。

参考讲解：

> 最终价值不是单点工具，而是可追踪的教师成长闭环。这里的试点数字均为 Mock 示例，用于展示未来评估方式；当前示例四维均值约从 61.5 提升到 77.5，均值差约 15.9 分，不展示没有原始样本支持的显著性或效应量结论。

---

## 四、现场容错方案

- **后端不可用**：保持页面当前结果，说明系统已自动降级到 Mock；不要现场修改代码或地址。
- **某个接口超过 10 秒**：继续使用回退结果，直接进入下一页。
- **图谱初始布局仍在运动**：立即切换到一个子域并用下拉框选择节点，不等待全局图稳定。
- **时间落后超过 15 秒**：诊断页只展示第一项问题并打开一次溯源；科研页只展示研究选题标签。
- **输入失误**：工作台使用“重置演示流程”；诊断与科研使用内置内容，不现场上传陌生文件。

---

## 五、讲解口径

1. 使用“可联调原型”“Mock 示例数据”“接口失败自动降级”等准确表述。
2. 只有真实后端成功响应时才说“当前展示为真实后端结果”。
3. 不把固定 Mock 数字称为实证结论，不宣称 `p < 0.01` 或 Cohen’s d。
4. 不宣称 DOCX/PDF、登录、数据库或知识图谱后端接口已经完成。
5. 演示主线始终围绕同一份“碳中和”教案，避免七页看起来像互不相关的页面集合。
