# STEM 教师教育智能体后端联调交接清单

版本：V1.0  
整理日期：2026-09-05  
最近一次真实联调记录：2026-09-02

本文面向后端开发、测试和部署同学，集中说明当前前端已经固定的接口契约、尚未接通的能力和联合验收方法。稳定字段的完整说明见[《API 接口文档》](API接口文档.md)；本文用于安排实现优先级和交付验收。

## 1. 前端现状与最近联调结论

前端七页工作台、页内功能目录、状态保留、Mock 演示模式和内容管理界面已经完成。当前后端联调重点不是新增页面功能，而是按现有契约补齐三个接口族，并对四种智能体场景重新做真实环境验收。

| 范围 | 2026-09-02 最近一次结果 | 当前结论 |
|---|---|---|
| 科研生成 `education_research` | 从 8502 网页发起，HTTP 200，方案正常显示 | 仅该场景当次验证成功，后端变更后仍需复测 |
| 诊断教师对话 `classroom_diagnosis` | 从诊断页发起，HTTP 200，对话正常显示 | 仅教师对话当次验证成功，诊断报告仍需复测 |
| 课程设计 `lesson_design` | 未完成三阶段真实验收 | 初生成、人工迭代、素养校验均需复测 |
| 教学模拟 `learning_support` | 未完成真实验收 | 课堂互动需复测 |
| 音频转写 | `POST /api/transcribe` 返回 HTTP 404 | 后端尚未接通 |
| 跨会话档案 | `GET /api/frontend-state/{workspace_id}` 返回 HTTP 404 | 后端尚未接通；PUT、DELETE 未对真实后端执行 |

真实请求失败时，前端保留输入、已有结果和当前流程阶段，并停留在发起操作的视图显示错误。如果该页面有 Mock 兜底，用户只能主动点击“查看 Mock 示例”进入只读预览；Mock 不覆盖业务数据，也不计作真实成功。

## 2. 优先级

| 优先级 | 后端工作 | 完成标准 |
|---|---|---|
| P0 | 稳定 `POST /api/agent-chat` | 四种 `agent_type` 及页面内各场景均满足固定请求、响应和错误契约 |
| P0 | 实现 `POST /api/transcribe` | 五种音频格式可转写，限制和错误均有明确 4xx JSON |
| P0 | 实现 GET、PUT、DELETE 档案接口 | 新档案 GET 返回空状态，完整覆盖、恢复和删除生命周期可用 |
| P1 | 身份、归属和并发控制 | 真实用户只能访问自己的 workspace，冲突不会静默覆盖 |
| P1 | 数据安全与运维能力 | 加密、保留与删除、日志脱敏、审计、限流、健康检查和错误追踪可用 |

## 3. P0：统一智能体接口

### 3.1 请求

```http
POST /api/agent-chat
Content-Type: application/json
X-Token: <可选测试令牌>
```

请求 Body 只能包含以下两个字段，不要求前端增加 `action`、会话 ID 或其他参数：

```json
{
  "question": "教师的问题及前端整理的必要上下文",
  "agent_type": "lesson_design"
}
```

- `question` 必须支持至少约 25,000 个 Unicode 字符。课程设计不同阶段、诊断报告和教师对话均通过其中的明确文字指令区分。
- `agent_type` 只能是 `learning_support`、`classroom_diagnosis`、`lesson_design`、`education_research` 之一。
- 前端连接超时为 10 秒、读取超时为 180 秒。网关、应用服务器和模型调用链应在 180 秒内完成响应，或尽早返回明确的非 2xx 错误。
- 当前前端请求已携带完成本轮生成所需的上下文，后端无需依赖额外会话状态。

### 3.2 成功响应

```json
{
  "answer": "可直接展示的完整 Markdown 回答",
  "agent_type": "lesson_design",
  "sources": ["课程标准（2022年版）"],
  "graph_sources": ["核心素养 → 跨学科实践"]
}
```

要求：

1. 使用 HTTP 2xx，Body 必须是 JSON 对象。
2. `answer` 必须是非空字符串。
3. `agent_type` 必须与请求完全一致。
4. `sources`、`graph_sources` 可省略；提供时必须是可直接展示的字符串数组。
5. 不得以 HTTP 200 返回空回答、错误对象、`success: false` 或错误的 `agent_type`。

### 3.3 失败响应

```json
{
  "detail": "模型服务暂时不可用，请稍后重试"
}
```

参数错误、鉴权失败、限流、模型失败或服务异常均使用对应的非 2xx 状态码和 JSON `detail`。不要返回 HTML 错误页、内部堆栈、密钥、模型配置或完整上游异常。

### 3.4 四种智能体的页面用途

| 页面场景 | `agent_type` | 输入规模与上下文 | `answer` 输出要求 |
|---|---|---|---|
| 教学模拟课堂互动 | `learning_support` | 主题、授课文本和本轮互动上下文 | 一条可直接展示的课堂反馈或学习支持建议 |
| 智能诊断报告 | `classroom_diagnosis` | 课例文本，前端文档导入最终最多发送约 20,000 字纯文本 | 完整、可读的循证诊断报告 |
| 诊断教师连续对话 | `classroom_diagnosis` | 课例摘录最多 2,400 字、匹配诊断最多 1,600 字、最近 6 条对话每条最多 900 字、本轮问题最多 2,000 字 | 针对本轮问题的教师支持回答 |
| 课程设计初生成 | `lesson_design` | 学段、主题、课时及生成要求 | 完整 Markdown 教案 |
| 课程设计人工迭代 | `lesson_design` | 当前教案和教师修改要求 | 修改后的完整 Markdown 教案 |
| 课程设计素养校验 | `lesson_design` | 当前教案及校验指令 | 清晰的校验结论和改进建议 |
| 科研孵化生成 | `education_research` | 教学痛点、示例或从诊断带入的内容 | 完整研究方案，可直接阅读和下载 |

## 4. P0：音频转写接口

```http
POST /api/transcribe
Content-Type: multipart/form-data
X-Token: <可选测试令牌>
```

文件字段固定为 `file`。支持 WAV、MP3、M4A、OGG、WebM，单文件不超过 50 MB。网关、反向代理和应用服务器的上传限制需要覆盖 50 MB 文件及 multipart 编码开销。

成功响应：

```json
{
  "text": "非空转写文本"
}
```

建议的失败状态为：空文件或损坏文件返回 400/422，超过上限返回 413，不支持格式返回 415；无论状态码如何，Body 均使用 JSON `detail`。成功时 `text` 不能为空字符串。

## 5. P0：跨会话档案接口

`workspace_id` 是前端生成的 UUID。当前阶段它用于定位档案，不代表用户认证。所有接口可选携带 `X-Token`。

### 5.1 读取

```http
GET /api/frontend-state/{workspace_id}
```

已有档案：

```json
{
  "state": {
    "sim_theme": "碳中和",
    "diag_lesson_text": "课例文本"
  }
}
```

合法的新 UUID 也必须返回 HTTP 200：

```json
{
  "state": {}
}
```

不能用 404 表示尚无档案，否则前端会将接口判断为未接通。

### 5.2 完整覆盖保存

```http
PUT /api/frontend-state/{workspace_id}
Content-Type: application/json
```

```json
{
  "state": {
    "sim_teacher_input": "授课文本",
    "diag_result": {"conclusion": "诊断内容"},
    "ws_lesson": "生成的 Markdown 教案",
    "research_result": {"title": "研究方案"}
  }
}
```

PUT 表示用本次 `state` 完整覆盖该 workspace 的当前档案，不是字段级合并。请求上限为 10 MB；超过上限必须明确拒绝，禁止静默截断。成功返回以下任一形式：

```json
{
  "success": true
}
```

或 HTTP 204。

### 5.3 删除

```http
DELETE /api/frontend-state/{workspace_id}
```

建议实现为幂等删除。成功返回 `2xx {"success":true}` 或 HTTP 204；后端确认成功后，前端才会清空本地并更换 workspace。

### 5.4 持久化内容范围

后端将 `state` 作为受 10 MB 大小限制的 JSON 对象完整保存并原样返回。当前前端会对恢复内容做字段和类型校验。

| 分组 | 典型内容 |
|---|---|
| 教学模拟 | 主题、授课草稿、转写文本、模拟轮次、反馈、信号和已生成结果 |
| 智能诊断 | 课例草稿、诊断报告、来源、输入快照、教师对话和问题草稿 |
| 课程设计 | 学段、主题、课时、教案、编辑稿、阶段、素养指标、校验结论和智能体结果 |
| 科研孵化 | 研究问题、输入来源、生成快照、研究方案和诊断带入内容 |
| 导航 | 当前七页主导航，用于恢复业务位置 |

明确不进入 `state` 的内容：

- 七页页内功能视图键、编辑/预览选择和临时 `_ui_` 控件状态；
- 原始音频字节、原始上传文件和上传控件对象；
- `workspace_id`、`STEM_API_TOKEN`、连接状态、后端错误详情；
- Mock 只读预览、待确认弹窗及其他临时界面状态。

## 6. 当前无需后端实现

- 知识图谱浏览页、61 个实体/关系及六个子域继续使用前端内置数据。
- 心流、多模态信号、ITRS 和 STEM 数值继续作为明确标注的本地演示指标。
- `trace`、`itrs`、`stem`、`stage` 等旧结构化字段不属于当前稳定 `/api/agent-chat` 返回契约。
- 后端无需解析前端上传的 TXT/DOCX 原文件；前端会提取并整理为 `question` 文本。

## 7. P1：正式上线前

1. 接入真实用户身份，为 workspace 建立归属关系并隔离跨用户访问。UUID 本身不能作为认证凭据。
2. 与前端共同设计版本号或 ETag 等并发写入控制。当前 PUT 是完整覆盖，后端不能单方面改变现有契约。
3. 落实课堂文本、诊断报告和教师对话的传输与存储加密、保留期限、删除策略、日志脱敏及访问审计。
4. 配置请求限流、实际文件类型检测、代理体积限制、模型调用超时、重试边界和错误追踪。
5. 提供后端健康检查、部署 BaseURL、`X-Token` 使用方法、测试环境和联调日志定位方法。

## 8. 后端交付物

- 可访问的测试 BaseURL、健康检查地址和环境可用时间；
- `X-Token` 是否启用、测试令牌的安全交付方式及鉴权失败示例；
- 三类接口的部署版本、启动方式和必要环境变量；
- 网关上传/Body 限制、模型超时配置和错误状态说明；
- 可按时间、workspace 或请求标识定位的脱敏日志方法；
- 后端自动化契约测试结果及已知限制。

## 9. 前后端联合验收

### 9.1 智能体

- 四种 `agent_type` 分别覆盖正常回答、空回答、类型不匹配、非 JSON、HTTP 错误和 180 秒读取超时。
- 课程设计覆盖初生成、人工迭代、素养校验；智能诊断覆盖报告生成和教师连续对话。
- 在 `STEM_MOCK_MODE=false` 下从 8502 网页发起真实请求。Mock 或受控替身响应不能代替真实联调结论。
- 失败后确认输入、已有结果、对话和流程阶段未丢失，页面未自动跳入结果视图。

### 9.2 音频转写

- WAV、MP3、M4A、OGG、WebM 各完成一次真实成功转写。
- 覆盖空文件、损坏文件、不支持格式、超过 50 MB 和网关限制。

### 9.3 档案生命周期

- 新 UUID GET 得到 `200 {"state":{}}`。
- PUT 后 GET 可原样恢复；再次 PUT 验证完整覆盖。
- DELETE 成功后 GET 回到空状态；验证重复 DELETE 的约定行为。
- 验证接近和超过 10 MB 的边界，不发生静默截断。
- 不使用生产数据，不在未确认环境执行真实档案删除。

### 9.4 固定联调顺序

```text
课程设计 → 智能诊断 → 教学模拟 → 科研孵化 → 音频转写 → 长期档案
```

每一项记录 BaseURL、版本、时间、请求场景、HTTP 状态和结论。仅以网页真实请求成功作为该场景的最终通过依据。
